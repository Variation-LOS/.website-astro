---
import Container from "src/components/container.astro";
import Layout from "src/layouts/Layout.astro";
import * as greatPost from "./markdowns/legal.md";
// Import statement
import { Content } from "./markdowns/legal.md";
import MarkdownStyles from "./markdowns/markdown-style.astro";

// import { remark } from "remark";
// import remarkToc from "remark-toc";
// import dayjs from "dayjs";
// import utc from "dayjs/plugin/utc";

// dayjs.extend(utc);

// const lastModified = dayjs()
//   .utc(Astro.props.frontmatter.lastModified)
//   .format("HH:mm:ss DD MMMM YYYY UTC");

// const file = await remark()
//   .use(remarkToc)
// //   .process(await read(Content));

// console.error(String(file));
---

<Layout title="Legal">
  <MarkdownStyles />
  <Container>
    <div class="Title">
      <h1>{greatPost.frontmatter.title}</h1>
      <h3>
        {greatPost.frontmatter.author}
        <!-- <p>{lastModified}</p> -->
      </h3>
    </div>
    <main>
      <div class="content">
        <Content />
      </div>
    </main>
  </Container>
</Layout>

<style>
  .Title {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
    margin: 48px 0;
  }

  .Title h1 {
    font-size: 48px;
    line-height: 1;
    font-weight: 500;
    margin: 0;
    letter-spacing: var(--title-font-letterspace) !important;
    border: none;
    padding: 0 14px;
    padding-top: 0;
    text-align: center;
  }

  .Title h3 {
    margin: 0;
    font-size: 16px;
    display: flex;
    align-items: center;
    flex-direction: row;
    gap: 10px;
    font-weight: 400;
    opacity: 0.75;
  }

  .Title p {
    margin: 0;
    font-size: 16px;
    color: var(--md-sys-color-on-background);
    display: flex;
    align-items: center;
    flex-direction: row;
    gap: 10px;
  }

  .Title p::before {
    content: "";
    margin: 0;
    height: 18px;
    width: 2px;
    display: block;
    background-color: var(--md-sys-color-on-background);
    opacity: 0.5;
    border-radius: 2px;
  }

  main {
    margin: 0;
    padding: 20px;
    box-sizing: border-box;
    font-size: 16px;
    line-height: 1.5;
    color: var(--md-sys-color-on-background);
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  @media (max-width: 640px) {
    main {
      padding: 14px;
    }
  }

  .content {
    max-width: 1200px;
  }
</style>

<!-- <script type="module">
//   import { remark } from "remark";
//   import remarkToc from "remark-toc";

//   const file = await remark()
//     .use(remarkToc)
//     .process(await read(Content));

//   console.error(String(file));
</script> -->
